import Button from '../../shared/components/Button';

export default function PageTest() {
  return (
    <div>
      <h1>Page de test</h1>
      <Button
        text="Participer"
        style="filled"
        color="primary"
        onClick={() => {}}
      />
    </div>
  );
}
